# 2026-04-16 KE-First Greenfield Foundation

## Planning Note

This branch marks a pivot away from the operator-first, workflow-heavy direction archived on `archive/operator-first-platform`.

Until a detailed implementation plan is written, treat the following as authoritative for the greenfield rebuild:

- branch: `ke-first-greenfield`
- spec: `docs/superpowers/specs/2026-04-16-ke-first-greenfield-foundation-design.md`

## Context Hygiene

To avoid confusing future agents and implementation work:

- do not assume prior onboarding, sponsor, claim/release, notification, or workflow-heavy specs describe the active direction
- use archived branches and archived specs only as reference material, not as the default product direction
- treat KE core quality as the primary goal:
  - ingestion
  - observation extraction
  - entity modeling
  - primary artifact curation
  - update/revision semantics
  - retrieval quality
- only pull ideas forward from the archive when they clearly improve the KE-first architecture

## Next Step

Write the full greenfield implementation plan against the KE-first foundation spec after the spec is explicitly approved.
